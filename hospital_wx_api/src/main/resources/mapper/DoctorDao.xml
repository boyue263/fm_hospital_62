<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.wx.dao.DoctorDao">
    <select id="selectDoctorInfoById" parameterType="int" resultType="java.util.HashMap">
    SELECT d.name AS name,
    d.photo AS photo,
    d.job AS job,
    d.remark AS remark,
    d.description AS description,
    p.price_1 AS price
    FROM doctor d
    JOIN doctor_price p ON d.id=p.doctor_id
    WHERE d.id = #{id}
    </select>
</mapper>