<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hospital.hms.dao.DeptDao">

    <select id="selectAll" resultType="java.util.HashMap">
        SELECT id, dept_name AS deptName
        FROM dept
        ORDER BY id
    </select>

    <select id="selectByCondition" parameterType="HashMap" resultType="java.util.HashMap">
        SELECT d.id, d.dept_name AS deptName, d.tel, d.email, d.`desc`, COUNT(u.id) AS emps
        FROM dept d
        LEFT JOIN users u ON d.id = u.dept_id AND u.status = 1
        <where>
            <if test="deptName != null and deptName != ''">
                AND d.dept_name LIKE CONCAT('%', #{deptName}, '%')
            </if>
        </where>
        GROUP BY d.id
        ORDER BY d.id
        LIMIT #{start}, #{length}
    </select>

    <select id="selectByConditionCount" parameterType="HashMap" resultType="java.lang.Long">
        SELECT COUNT(*) FROM (
        SELECT d.id FROM dept d
        LEFT JOIN users u ON d.id = u.dept_id AND u.status = 1
        <where>
            <if test="deptName != null and deptName != ''">
                AND d.dept_name LIKE CONCAT('%', #{deptName}, '%')
            </if>
        </where>
        GROUP BY d.id
        ) AS temp
    </select>

    <insert id="insertDept" parameterType="com.hospital.hms.pojo.Dept" >
        INSERT INTO dept SET dept_name = #{deptName}
        <if test="tel !=null and tel !='' ">
            ,tel = #{tel}
        </if>
        <if test="tel !=null and tel !='' ">
            ,email = #{email}
        </if>
        <if test="tel !=null and tel !='' ">
            ,`desc` = #{desc}
        </if>
    </insert>

    <select id="selectDeptById" parameterType="Integer" resultType="java.util.HashMap">
        select id , dept_name AS deptName ,tel ,email,`desc` FROM dept
        WHERE id = #{deptId}
    </select>

    <update id="updateDept" parameterType="com.hospital.hms.pojo.Dept">
        UPDATE dept
        SET dept_name=#{deptName},
            tel=#{tel},
            email=#{email},
            `desc`=#{desc}
        WHERE id=#{id}
    </update>

    <delete id="deleteDeptById" >
        DELETE FROM dept
        WHERE id IN
            <foreach collection="array" item="id" open="(" separator=","  close=")">
                #{id}
            </foreach>
    </delete>

    <select id="selectCanDelete" resultType="boolean">
        SELECT IF(SUM(temp.users)>0,FALSE,TRUE) AS result
        FROM ( select COUNT(u.id) AS users
        FROM dept d
        JOIN users u ON d.id = u.dept_id
        WHERE d.id IN
            <foreach collection="array" item="one" open="(" separator="," close=")">
                #{one}
            </foreach>
        GROUP BY d.id
        ) temp
    </select>


</mapper>
